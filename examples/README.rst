Examples and tutorials
======================

The `examples/` directory contains tutorial notebooks and scripts. For
development, install the package in editable mode and run the examples:

    $ python -m pip install -e .
    $ python examples/tutorial.py

Or open `examples/tutorial.ipynb` with Jupyter Notebook.
